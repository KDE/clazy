qdeleteall

Finds places where a call to qDeleteAll has a redundant values() call, for example:
    QSet<QString *> s;
    qDeleteAll(s.values());
instead of
    QSet<QString *> s;
    qDeleteAll(s);

